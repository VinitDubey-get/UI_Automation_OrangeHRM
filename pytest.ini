[pytest]
markers =
    smoke: Critical path tests — run first
    regression: Full suite
    negative: Error and edge cases
    employee: PIM module tests
    leave: Leave module tests
    recruitment: Recruitment module tests

addopts = --alluredir=allure-results --clean-alluredir -v --tb=short
testpaths = tests
timeout = 30